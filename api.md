你问的是 **相联目录表**，这类题核心就 3 个量：

```text
1. 行数：目录表有多少行
2. 比较位数：比较器每次比较多少位
3. 总位数：目录表一共要存多少 bit
```

它们都来自同一个东西：**主存地址和 Cache 地址怎么拆分**。

---

## 一、先记通用步骤

### 第一步：算块大小

如果题目说“一个主存周期取一个 Cache 块”，并且主存是 m 体交叉，每个分体宽度是 W，那么：

```text
Cache 块大小 = m × W
```

例如补充习题 1 里，主存模 4 交叉，每个分体宽 32 位，也就是 4B，所以一个周期可取：

```text
块大小 = 4 × 4B = 16B
```

---

### 第二步：算主存地址位数

如果主存容量是 256KB，按字节编址：

```text
256KB = 256 × 1024B = 2^8 × 2^10B = 2^18B
```

所以：

```text
主存地址位数 = 18 位
```

---

### 第三步：算块内位移

块大小是 16B：

```text
16B = 2^4B
```

所以：

```text
块内位移 = 4 位
```

---

### 第四步：算主存块地址位数

```text
主存块地址位数 = 主存地址位数 - 块内位移位数
```

也就是：

```text
主存块地址位数 = 18 - 4 = 14 位
```

这个 14 位表示：主存一共有 2^14 个块。

---

## 二、如果是“全相联目录表”

题 7.16 就是这种类型。它的答案里明确写了：主存容量 1M 字，所以主存地址 20 位；Cache 容量 4K 字，所以 Cache 地址 12 位；8 个存储体低位交叉，所以块大小为 8 个字，块内位移 3 位；因此主存块地址为 17 位，Cache 块地址为 9 位。相联目录表行数等于 Cache 块数，即 2^9 = 512 行；比较位数等于主存块地址位数，即 17 位；总位数等于行数 × 宽度 = 512 × 27 = 13824 位。

### 全相联的计算模板

全相联的意思是：

```text
主存任意块可以放到 Cache 任意块
```

所以目录表要记录：

```text
这个 Cache 块里现在放的是哪个主存块
```

因此：

```text
目录表行数 = Cache 块数
```

比较的时候，因为没有组号/index，必须拿主存块地址整体去比较，所以：

```text
比较位数 = 主存块地址位数
```

总位数按你课件题 7.16 的写法：

```text
每行宽度 = 主存块地址位数 + Cache块地址位数 + 有效位
总位数 = 行数 × 每行宽度
```

---

### 用题 7.16 走一遍

题目：

```text
主存容量 = 1M 字 = 2^20 字
Cache容量 = 4K 字 = 2^12 字
8 个存储体低位交叉
一个主存周期取一个块
```

块大小：

```text
块大小 = 8 字 = 2^3 字
```

主存块地址位数：

```text
20 - 3 = 17 位
```

Cache 块地址位数：

```text
12 - 3 = 9 位
```

Cache 块数：

```text
2^9 = 512 块
```

所以：

```text
目录表行数 = 512 行
比较位数 = 17 位
```

每行宽度：

```text
17 + 9 + 1 = 27 位
```

其中：

```text
17：主存块地址
9：Cache块地址
1：有效位
```

总位数：

```text
512 × 27 = 13824 位
```

---

## 三、如果是“组相联目录表”

补充习题 1 是这种类型。题目说采用组相联映像，Cache 大小 1KB，主存模 4 交叉，每个分体宽 32 位，主存总容量 256KB，并约定使用 4 个外相等比较电路，要求设计相联目录表。

这里的关键是：

```text
4 个比较电路 = 4 路组相联
```

因为一组里面有 4 个候选块，所以需要 4 个比较器同时比较。

---

## 四、组相联的计算模板

设：

```text
n = 组内块数，也就是几路组相联
G = Cache 组数
```

那么：

```text
Cache 块数 = Cache 容量 / 块大小
组数 G = Cache 块数 / n
目录表行数 = 组数 G
```

组相联地址结构是：

```text
主存地址 = 标识 tag + 组号 index + 块内位移 offset
```

所以：

```text
比较位数 = tag 位数
```

因为组号已经用来选中了目录表的某一行，块内位移只表示块内哪个字节，真正需要比较的只剩 tag。

总位数常见有两种口径：

### 不计有效位

```text
总位数 = 组数 × 组内块数 × tag位数
```

### 计有效位

```text
总位数 = 组数 × 组内块数 × (tag位数 + 1)
```

考试时如果题目没明确，可以写：

```text
若不计有效位，总位数为……
若计有效位，总位数为……
```

这样最稳。

---

## 五、补充习题 1 完整计算

题目条件：

```text
Cache大小 = 1KB
主存容量 = 256KB
主存模4交叉
每个分体宽 = 32位 = 4B
4个外相等比较电路
```

### 1. 块大小

```text
块大小 = 4 × 4B = 16B
```

---

### 2. 主存地址位数

```text
256KB = 2^18B
```

所以：

```text
主存地址位数 = 18 位
```

---

### 3. 块内位移

```text
16B = 2^4B
```

所以：

```text
块内位移 = 4 位
```

---

### 4. Cache 块数

```text
Cache大小 = 1KB = 2^10B
块大小 = 16B = 2^4B

Cache块数 = 2^10 / 2^4 = 2^6 = 64 块
```

---

### 5. 组数

因为有 4 个比较器，所以是 4 路组相联：

```text
组内块数 = 4
```

因此：

```text
组数 = 64 / 4 = 16 组
```

所以：

```text
目录表行数 = 16 行
```

---

### 6. 组号位数

```text
16组 = 2^4组
```

所以：

```text
组号 = 4 位
```

---

### 7. tag 位数，也就是比较位数

主存地址总共 18 位：

```text
主存地址 = tag + 组号 + 块内位移
```

所以：

```text
tag = 18 - 4 - 4 = 10 位
```

因此：

```text
每个比较电路的位数 = 10 位
```

---

### 8. 总位数

每一行有 4 个 tag，每个 tag 是 10 位。

不计有效位：

```text
总位数 = 16 × 4 × 10 = 640 位
```

计有效位：

```text
总位数 = 16 × 4 × (10 + 1)
       = 16 × 4 × 11
       = 704 位
```

---

## 六、这类题最后答案怎么写

补充习题 1 可以写成：

```text
块大小 = 4 × 32位 = 128位 = 16B

主存容量 = 256KB = 2^18B
主存地址位数 = 18位

块内位移 = log2 16 = 4位

Cache容量 = 1KB = 2^10B
Cache块数 = 2^10 / 2^4 = 2^6 = 64块

由于采用4个外相等比较电路，所以为4路组相联。
组数 = 64 / 4 = 16组
目录表行数 = 16行

组号位数 = log2 16 = 4位
标识位数 = 18 - 4 - 4 = 10位

每个比较电路位数 = 10位

目录表总位数：
不计有效位：16 × 4 × 10 = 640位
计有效位：16 × 4 × 11 = 704位
```

---

## 七、一句话记忆

```text
全相联：行数 = Cache块数，比较位数 = 主存块地址位数。

组相联：行数 = 组数，比较位数 = tag位数。

总位数 = 行数 × 每行存的内容位数。
```

你现在先别急着背完整定义，做题时只要盯住这三个字段：

```text
tag + index + offset
```

其中：

```text
offset 由块大小决定
index 由组数决定
tag = 总地址位数 - index - offset
```
